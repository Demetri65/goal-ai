# SMART-GoT Web Console

## Dev

```bash
pnpm dev
```

Run from the repo root. For a web-only dev session:

```bash
pnpm dev:web
```

Set API origin if needed:

```bash
NEXT_PUBLIC_SMARTGOT_API=http://127.0.0.1:8001
```
